Created CSV folders to contain Data files(employee and vehicle collision are huge files) and Output csv files. I have uploaded an Image file as 
I took the .py file and ran in SPYDER IDE just to check if my .py files were running.
The image (running_demo_for_py_files.PNG) contains a successful execution for the same 
